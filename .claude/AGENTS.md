# Coding Style

## Control Flow

- Never use `else` after an `if` statement - use early returns or guard clauses
- `if let Some(...) else` is acceptable for pattern matching

## Principles

- Clean code
- SOLID principles
- Isolated pure functions
- Idiomatic Rust
